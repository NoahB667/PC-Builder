spring:
  application:
    name: pc-builder

server:
  port: 8080

# TODO: CORS Configuration - Update this when deploying to production
cors:
  allowed-origins: http://localhost:5173,http://localhost:3000,https://your-production-domain.com

logging:
  level:
    com.pcbuilder: DEBUG
    org.hibernate.SQL: DEBUG
